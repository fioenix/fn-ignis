# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg
.coverage
.coverage.*
htmlcov/
.pytest_cache/
.ruff_cache/


# Virtual Environments
.venv/
venv/
ENV/
env.bak/
venv.bak/

# Environment Variables & Secrets
.env
# Any copy of it is the same secret. A timestamped backup sat untracked and
# committable during the 20/09/2026 cutover.
.env.*
!.env.example
.env.local
.env.*.local
.mcp.json
*.pem
*.key


# Local Notes, Ephemeral Handoffs & Private Backlogs
*.local.md
*.handoff.md
*.ephemeral.md
*.tmp.md
.local/
.handoff/
docs/ephemeral/
backlog.local.md

# Research Workspaces -- local-first, never auto-committed or auto-published.
# `.ignis/research/<slug>/` holds a workspace manifest, run journals and derived artifacts that
# may contain raw source material. Exporting one is an explicit user action, not a side effect.
.ignis/

# Database & Local Storage
*.sqlite
*.sqlite3
*.db
data/
pgdata/


# IDE & OS
.DS_Store
.idea/
.vscode/
.cursor/mcp.json
.cursor/*.json
*.swp
*.swo
*.backup_*.json

# Generated artifacts — reports/ is local output only, never committed.
# Curated sample dossiers live in examples/case-studies/ instead.
reports/*
!reports/.gitkeep
src/reports/
supabase/.temp/
docs/AUDIT-*.md
docs/LAUNCH-*.md
backup_*.json

*storage_state*.json
*session*.json
scratch/

